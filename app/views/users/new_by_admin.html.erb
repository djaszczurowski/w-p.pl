<%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label "Użytkownik" %><br />
      <%= f.text_field :username %>
    </div>
    <div class="field">
      <%= f.label :email %><br />
      <%= f.text_field :email %>
    </div>
    <div class="field">
      <%= f.label :avatar %><br />
      <%= f.text_field :avatar %>
    </div>
    <div class="field">
      <%= f.label "Hasło" %><br />
      <%= f.password_field :password %>
    </div>
    <div>
      <%= f.label "Powtórz hasło" %><br/>
      <%= f.password_field :password_confirmation%>
    </div>
    <div>
      <%= f.label "Poziom uprawnień" %><br/>
      <%= f.password_field :privilegesLevel%>
    </div>
    <div class="actions"><br />
      <%= f.submit "Zapisz"%>
    </div>
  <% end %>

